fun main(args: Array<String>) {
    //the caesar ciper
    //variable initialization
    val s = "blackholesarewheregoddivideodvyzero"
    println("original message: $s")
    var message = ""
    var shift = 1

    //Loop iterating over characters
    for (c in s) {
        var i = c.toString()
        i += shift
        if (i > 'z'.toString())

        //printing
        println("Encerypted message")
    }
}